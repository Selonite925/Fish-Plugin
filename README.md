# Fish-plugin

适用于 TRSS-Yunzai / Yunzai-Bot 的独立钓鱼插件。

这个插件提供一套完整的群聊钓鱼玩法：每日钓鱼、鱼缸收藏、鱼市售鱼、鱼币商店、鱼竿鱼饵、传说鱼炼竿、每日鱼讯、钓鱼成就和图片面板。

## 功能概览

- `#钓鱼`：每日抛竿钓鱼，有概率钓到不同稀有度的鱼。
- `#钓鱼极速版`：一次结算当前所有可用钓鱼次数，跳过演出并用图片汇总结果。
- `#查看鱼缸`：查看收藏的鱼，支持升级鱼缸容量。
- `#鱼市`：出售鱼获换鱼币，购买鱼饵、鱼竿和额外钓鱼券。
- `#鱼竿 / #换竿`：切换不同鱼竿，影响等待时间、上鱼率、失败保护和稀有鱼倾向。
- `#鱼饵 / #换饵`：切换不同鱼饵，鱼饵会在钓鱼时消耗。
- `#炼竿`：消耗鱼缸里的 `legendary` 鱼，炼成特殊鱼竿。
- `#限时鱼讯`：每天刷新高活跃鱼，钓中可获得额外鱼币。
- `#钓鱼成就`：查看成就和奖励。
- `#钓鱼管理`：查看管理、维护和主人命令。

## 安装

进入你的 Yunzai-Bot 或 TRSS-Yunzai 根目录，然后执行：

```bash
git clone --depth=1 https://github.com/Selonite925/Fish-Plugin.git ./plugins/Fish-plugin
```

拉取完成后重启机器人。

如果你的环境使用 pnpm 管理依赖，通常不需要额外安装依赖；插件会使用 Yunzai 已有的运行环境和 puppeteer 图片渲染能力。

## 更新

进入机器人根目录，执行：

```bash
git -C ./plugins/Fish-plugin pull
```

更新后重启机器人。

## 卸载

停止机器人后删除插件目录：

```bash
rm -rf ./plugins/Fish-plugin
```

Windows PowerShell 可用：

```powershell
Remove-Item -Recurse -Force .\plugins\Fish-plugin
```

## 第一次使用

在群里发送：

```text
#钓鱼帮助
```

常用起步流程：

```text
#钓鱼
#查看鱼缸
#鱼市
#售鱼 common
#鱼市购买 鱼饵1
#换饵 鱼饵1
#鱼市购买 鱼竿1
#换竿 鱼竿1
```

## 常用命令

### 基础钓鱼

| 命令 | 说明 |
| --- | --- |
| `#钓鱼` | 抛竿钓鱼 |
| `#钓鱼极速版` | 一次钓完当前所有可用次数，并以图片汇总结果 |
| `#钓鱼帮助` | 查看帮助 |
| `#钓鱼次数` | 查看今日剩余次数 |
| `#查看鱼缸` | 查看鱼缸和收藏 |
| `#限时鱼讯` | 查看今日高活跃鱼 |
| `#钓鱼成就` | 查看钓鱼成就 |

### 鱼市和售鱼

| 命令 | 说明 |
| --- | --- |
| `#鱼市` | 查看商店、鱼饵、鱼竿和售鱼提示 |
| `#售鱼 1` | 卖出今日渔获列表第 1 条 |
| `#售鱼 common` | 卖出今日所有 common 鱼 |
| `#售鱼 common 3` | 卖出今日 common 列表第 3 条 |
| `#售鱼 鱼缸3` | 卖出鱼缸展示列表第 3 条 |
| `#售鱼 全部` | 卖出今日所有可售鱼 |

`legendary` 和彩蛋鱼 `？` 不能出售。

售鱼价格以不可售卖的 `legendary` 和彩蛋鱼为基准：二者合计上鱼概率为 2%，统一视作 1500 价值，基准期望为 30 鱼币/次成功上鱼。可售鱼按稀有度概率反推价值区间，当前区间为：`common` 35-80、`uncommon` 75-165、`rare` 150-330、`epic` 280-620。单条鱼价格会根据该鱼在本鱼种长度、重量范围内的位置浮动，重量占 70%、长度占 30%，最终始终限制在对应稀有度区间内。

### 买东西

| 命令 | 说明 |
| --- | --- |
| `#鱼市购买 鱼饵1` | 购买鱼市里的第 1 种鱼饵 |
| `#鱼市购买 鱼饵1*5` | 批量购买 5 包鱼饵 |
| `#鱼市购买 鱼饵1 5包` | 同样是批量购买 5 包鱼饵 |
| `#鱼市购买 鱼竿1` | 购买鱼市里的第 1 根鱼竿 |
| `#鱼市购买 额外钓鱼券` | 购买额外钓鱼次数 |
| `#鱼市购买 自定义鱼饵 桂花酒糟` | 用文本定制一份鱼饵 |

鱼饵价格按“包”计算；每包会提供 `packSize` 次库存，例如 1 包 3 份的鱼饵可以在钓鱼时消耗 3 次。

额外钓鱼券只在基础次数用完后消耗，并单独记录为额外竿；鱼缸升级后新增的基础次数仍会按基础竿补给玩家。

### 鱼竿

| 命令 | 说明 |
| --- | --- |
| `#鱼竿` | 查看可见鱼竿列表 |
| `#换竿 0` | 换回默认的新手竹竿 |
| `#换竿 默认` | 换回默认的新手竹竿 |
| `#换竿 1` | 切换鱼竿列表第 1 根 |
| `#换竿 鱼竿1` | 切换鱼竿列表第 1 根 |
| `#换杆 鱼竿1` | `换杆` 也可以识别 |
| `#换竿 疾风短竿` | 按鱼竿名切换 |

鱼竿列表里的 `鱼竿1` 和鱼市里的 `鱼竿1` 对齐。默认的新手竹竿单独用 `0` 或 `默认` 切回。

### 鱼饵

| 命令 | 说明 |
| --- | --- |
| `#鱼饵` | 查看鱼饵列表和库存 |
| `#换饵 0` | 换回默认的清水团饵 |
| `#换饵 默认` | 换回默认的清水团饵 |
| `#换饵 清水` | 换回清水团饵 |
| `#换饵 1` | 切换鱼饵列表第 1 种 |
| `#换饵 鱼饵1` | 切换鱼饵列表第 1 种 |
| `#换饵 饵料1` | 切换鱼饵列表第 1 种 |
| `#换饵 沉流鱼饵` | 按鱼饵名切换 |

鱼饵列表里的 `鱼饵1` 和鱼市里的 `鱼饵1` 对齐。默认的清水团饵单独用 `0` 或 `默认` 切回。

### 炼竿

| 命令 | 说明 |
| --- | --- |
| `#炼竿 3` | 用鱼缸第 3 条 legendary 鱼炼竿 |
| `#炼竿神龙` | 用名为神龙的 legendary 鱼炼竿 |
| `#炼竿 神龙 2` | 有多条同名 legendary 时使用第 2 条 |

每种 `legendary` 鱼对应一根特殊鱼竿。已经拥有对应鱼竿时，不会重复炼成。

### 鱼缸升级

鱼缸每升 1 级增加 5 格容量，并让玩家本人每日基础钓数永久 +2。升级消耗会随等级递增：升到第 N 级需要 N 条 `legendary` 鱼，或 3N 条 `epic` 鱼。

### 管理命令

| 命令 | 说明 |
| --- | --- |
| `#钓鱼管理` | 查看管理命令 |
| `#封竿` | 主人在当前群停用本插件 |
| `#同步鱼缸` | 同步和修正鱼缸数据 |
| `#修复鱼数据` | 修复用户数据结构 |
| `#补鱼 @某人 rare 鳗鱼 80 3.5` | 主人补发鱼获 |
| `#鱼币补偿 @某人 100` | 主人补发鱼币 |

## 玩法说明

### 稀有度

鱼的稀有度从低到高大致为：

```text
common -> uncommon -> rare -> epic -> legendary -> ？
```

`？` 是彩蛋稀有度，出现率非常低。

### 鱼竿怎么选

- 想钓得快：优先买 `疾风短竿`、`柳影细竿`。
- 想少空竿：优先买 `稳钓重竿`。
- 想追稀有鱼：优先买 `猎珍长竿`。
- 想守大鱼：优先买 `阔鳞量竿`、`坠岩守竿`。

### 鱼饵怎么选

- 想稳定上鱼：买 `香谷鱼饵`。
- 想多钓普通鱼攒鱼币：买 `银鳞鱼饵` 或 `青苔鱼饵`。
- 想追稀有鱼：买 `沉流鱼饵`、`乌贼鱼饵`、`雷息鱼饵`。
- 想玩随机效果：买 `自定义鱼饵`。

## 图片面板

以下命令会优先使用 TRSS/Yunzai 的 puppeteer 渲染图片面板：

```text
#钓鱼帮助
#鱼市
#查看鱼缸
#钓鱼成就
#鱼竿
#鱼饵
```

可选本地背景目录：

```text
plugins/Fish-plugin/resources/backgrounds
```

把 `.jpg`、`.jpeg`、`.png`、`.webp` 放进去后，图片面板会自动随机使用。

## 数据文件

插件数据主要保存在：

| 文件 | 说明 |
| --- | --- |
| `fishdata/fishData.json` | 用户主数据、鱼缸、鱼币、鱼竿、鱼饵、成就 |
| `fishdata/fishConfig.json` | 基础配置、封竿群 |
| `fishdata/baitData.json` | 手动打窝数据 |
| `fishdata/lostItems.json` | 失败事件掉落物 |
| `fishdata/worldState.json` | 每日鱼讯状态 |

更新插件前建议备份 `fishdata/*.json`。

## 自定义和二次修改

常改位置：

| 文件 | 内容 |
| --- | --- |
| `fishdata/fishpool.js` | 鱼池、鱼种、稀有度概率、随机事件 |
| `lib/constants.js` | 鱼竿、鱼饵、商店、成就、经济数值 |
| `lib/custom-bait.js` | 自定义鱼饵生成规则 |
| `lib/signals.js` | 每日鱼讯逻辑 |
| `resources/panel/panel.html` | 图片面板模板 |

## 常见问题

### 安装后没有反应

先确认插件目录是：

```text
plugins/Fish-plugin
```

然后重启机器人。不要把仓库套成 `plugins/Fish-plugin/Fish-Plugin/Fish.js` 这种多一层目录。

### 怎么切回默认鱼竿和鱼饵

```text
#换竿 0
#换饵 0
```

也可以用：

```text
#换竿 默认
#换饵 默认
```

### 鱼饵为什么会减少

非默认鱼饵是消耗品。装备后每次钓鱼会消耗库存，用完会自动回到清水团饵。

### 特殊鱼竿怎么买

特殊鱼竿不能直接购买，需要钓到对应的 `legendary` 鱼，并把它放在鱼缸里执行 `#炼竿`。

## 开源地址

GitHub：

```text
https://github.com/Selonite925/Fish-Plugin
```
